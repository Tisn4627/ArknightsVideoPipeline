# ArknightsVideoPipeline 依赖
# 安装: pip install -r requirements.txt
# 注: 版本约束以 pyproject.toml 为单一事实源，本文件保持同步

opencv-python>=4.8.0,<5
numpy>=1.24.0,<3
movielite>=0.1.0,<1
pictex>=0.1.0,<1
Pillow>=10.0.0,<12
tqdm>=4.65.0,<5
PyQt6>=6.6.0,<7
